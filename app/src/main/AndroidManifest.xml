<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leverx.android_modern_architecture_sample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ArticleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidModernArchitectureSample"
        android:windowSoftInputMode="adjustPan">
        <activity
            android:name=".ui.main.view.MainActivity"

            android:exported="false" />
        <activity
            android:name=".translateapp.MainActivityQuiz"

            android:exported="false" />
        <activity
            android:name=".ui.compose.MainComposeActivity"
            android:exported="false"
            android:label="@string/title_activity_main_compose"
            android:theme="@style/Theme.AndroidModernArchitectureSample.NoActionBar" />
        <activity
            android:name=".MasterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>